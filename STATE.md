# Current State — ProxyPilot-public

_As of commit `b634256` on `main` (2026-07-13). Generated by /groundhog; regenerated each grounding pass — do not hand-edit. Every line below describes state as of that commit — diff it against live `git rev-parse HEAD` to see how many commits have landed since; a gap is expected staleness, not an error._

- **Version:** 1.11.7 (build 40) — source of truth: `project.yml` (`MARKETING_VERSION`/`CURRENT_PROJECT_VERSION`); tag `v1.11.7` exists.
- **In flight:** nothing — clean worktree, `main` even with `origin/main`.
- **Last landed:** `b634256` "Bump MARKETING_VERSION to 1.11.7 in project.yml" (2026-07-13), preceded by `ea308f0` "v1.11.7: Interactive Dock Tile, session count fix, and model list scrolling".
- **Broken / blocked:** none known — no `docs/bulletins/active/`, no failing-test references in tree.
- **Next move:** safe to proceed; this is a public mirror with no AGENTS.md/CLAUDE.md/devlog/punch-card governance — no handoff ceremony required. Verify any change against the private source before treating this repo as authoritative.

> Snapshot, not gospel — anchored to the commit above, not necessarily live HEAD. Public mirror; the private ProxyPilot repo is the canonical source. Re-run /groundhog to refresh.
